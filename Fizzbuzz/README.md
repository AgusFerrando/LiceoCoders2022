# Nuestra primera Kata: Fizzzbuzz

Vamos a crear nuestro proyecto en **node** con jest desde el principio

```bash
npm init -y  #-y me responde automaticamente las preguntas de config

npm i -D jest

npx jest --init
```